<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>Registration</title>
    <link rel="stylesheet" href="/css/main.css">
</head>

<body>
    <%- include('blocks/header') -%>
        <main>
            <h1>Регистрация</h1>
        </main>
        <div class="container">
            <div id="error-messages" class="error-container">
                <% if (messages && messages.length> 0) { %>
                    <ul>
                        <% messages.forEach(function(message) { %>
                            <li>
                                <%= message %>
                            </li>
                            <% }) %>
                    </ul>
                    <% } %>
            </div>
            <div class="regisrtarion">
            <form action="/register" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                    <label for="newUsername">New Username</label>
                    <input type="text" id="newUsername" name="username" autocomplete="newUsername">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name="email" autocomplete="email">
                    <label for="newPassword">New Password</label>
                    <input type="password" id="newPassword" name="password" autocomplete="newPassword">
                    <button type="submit">Register</button>
            </form>
            </div>
            <div class="toggle-theme">
                <button id="toggleThemeButton">Toggle Theme</button>
            </div>
        </div>
        <footer>
            <p>Все права защищены &copy; 2024</p>
          </footer>
</body>

</html>